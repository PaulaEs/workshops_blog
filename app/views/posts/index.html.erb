<% title "Posts" %>

<table>
  <tr>
    <th>Content</th>
    <th>Title</th>
  </tr>
  <% for post in @posts %>
    <tr>
      <td><%= post.content %></td>
      <td><%= post.title %></td>
      <td><%= link_to "Show", post %></td>
    </tr>
  <% end %>
</table>

